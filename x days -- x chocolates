#include <stdio.h>
int main()
{
    int t,n,count;
    scanf("%d\n", &t);
    {
        for(int i=0;i<t;i++)
        scanf("%d\n",&n);
        count = 0;
        for(int j=1;j<=n;j+=2)
        {
            count += j;
        }
        printf("%d\n" , count);
    }
return 0;
}
